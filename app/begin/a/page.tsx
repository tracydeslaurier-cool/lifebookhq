import { ThresholdExperience } from "@/components/threshold/ThresholdExperience";

// Variant A — Radical Simplicity: logo, voice, text, globe. Nothing else.
export default function Page() {
  return <ThresholdExperience variant="a" />;
}
