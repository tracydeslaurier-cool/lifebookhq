import { OpeningExperience } from "@/components/opening/OpeningExperience";

// The experience that used to live at "/". Still behind the staging gate.
export default function Enter() {
  return <OpeningExperience />;
}
